# QQ群管机器人 · 维护手册

> 日常维护、故障排查、版本管理、备份恢复。配合 `说明.md`（部署）一起使用。

---

## 一、日常操作

### 启动 / 停止
| 操作 | 方法 |
|---|---|
| 启动 | 双击 `启动机器人.bat`（看门狗模式：自动拉起并守护 NapCat + NoneBot） |
| 停止 | 关闭那个控制台窗口（协议端与机器人会一并退出），或在窗口里按 `Ctrl+C` |
| 检查存活 | 群里发 `ping` 或 `存活`；也可运行 `python deploy\watchdog.py --status` |
| 手动重新登录 | 双击 `重新登录.bat`（或 `python deploy\watchdog.py --relogin`） |
| 部署自检 | 双击 `一键自检.bat`（9项检查，绿色才健康） |
| 开启/关闭开机自启 | 双击 `开启开机自启.bat` / `关闭开机自启.bat`（或 `python deploy\autostart.py install\|uninstall\|status`） |

### 睡眠 / 休眠 与开机自启
- **睡眠（含笔记本"现代待机"）**：进程被冻结、网卡断电、所有连接断开——包括 QQ 连接和本地 WebSocket
- **休眠**：内存写入硬盘、整机断电，等同于关机再开机
- 睡醒后的后果：协议端连接断开、QQ 可能判定登录失效；**睡眠期间的消息（含入群申请、广告拦截）收不到**
- 看门狗的应对：每 15 秒巡检，一旦发现巡检间隔异常拉长（>60 秒）就判定"刚睡醒"，立刻体检——
  - 协议端在线 → 重启机器人服务刷新本地连接
  - 协议端离线/登录失效 → 立即尝试重新登录（跳过冷却）
  - 需要扫码时照例把二维码放到项目根目录
- 想看效果：`python deploy\watchdog.py --simulate-wake`（模拟一次唤醒自愈，结果见 `logs/watchdog.log`）
- 若希望它长时间在线：把电源计划里“接通电源时”的睡眠/休眠设为“从不”，别用睡眠代替关机
- 开机自启说明：`开启开机自启.bat` 会在「启动」文件夹放一个最小化快捷方式，**登录 Windows 后自动运行**；
  若想“开机即启、无需登录”，用管理员身份执行（把 `<项目目录>` 换成实际路径）：
  ```
  schtasks /Create /TN "QQ群管机器人" /SC ONSTART /RU SYSTEM /RL HIGHEST ^
    /TR "python \"<项目目录>\deploy\watchdog.py\""
  ```

### 看门狗（watchdog）做了什么
`启动机器人.bat` 实际运行的是一条常驻守护进程，每 15 秒巡检一次：

1. **进程守护**：NapCat 或机器人服务挂掉 → 自动重新拉起（日志见 `logs/`）
2. **登录失效自动重登**：检测到“账号登录已失效” → 自动重启协议端尝试**免扫码**快速登录（最多快速重试 3 次，之后每 15 分钟慢速重试）
3. **睡眠唤醒自愈**：系统睡眠/休眠结束后立刻体检并自愈（见上一节）
4. **必须人工扫码时提醒**：如果 QQ 要求重新扫码（免扫码失败），会自动把**最新二维码**同步到项目根目录：
   - `需要扫码登录.png` —— 手机 QQ 扫码用（每 2 分钟自动更新为最新码）
   - `需要扫码登录.txt` —— 处理说明
   - **扫码成功后这两个文件会自动消失**
5. **多开保护**：发现已有协议端/机器人服务在运行（上次残留）时只监控、不重复启动，避免端口/账号冲突

> 状态检测方式：优先通过 NapCat 的 HTTP 接口（127.0.0.1:3000，见 `部署配置.json` 的 `napcat_http_port`/`napcat_http_token`）查询登录状态，接口不可用时回退到日志分析。

### 历史日志在哪
| 文件 | 内容 |
|---|---|
| `logs/watchdog.log` | 看门狗动作记录（拉起进程、重登、扫码提醒） |
| `logs/napcat.log` | 协议端输出（登录、二维码、收发消息） |
| `logs/bot.log` | 机器人服务输出（命令执行、报错） |

### 两个窗口的职责
- 现在只有一个窗口（看门狗），协议端与机器人的输出都写入 `logs/` 下的日志文件
- 想看实时输出：`python deploy\watchdog.py` 前台运行即可（会滚动打印守护日志）

---

## 二、常见故障排查（FAQ）

### 1. 群里发命令没反应
按顺序排查：
1. 群里发 `ping` —— 有回复说明机器人在线，是命令本身问题
2. `ping` 也没反应 → 看机器人窗口有没有 `Bot <机器人QQ> connected`；没有 → NapCat 没连上（重启两个程序）
3. 身份检查：管理类命令需要 管理员/群主/超级管理员，普通成员发了不会响应（这是设计行为）
4. 拼写检查：命令要以 `/` 开头（如 `/禁言 xxx`），`/解出拉黑` 这种错别字不会触发（正确是 `/解除拉黑`）
5. 连 `/ping` 都没反应 → 检查 `qq-group-bot/.env` 是否有 `COMMAND_START=["/"]`（v1.8.0 起只认 `/命令` 形式；`bot.py` 里有兜底声明，但建议保留这行）

> 注意：v1.8.0 起**裸命令已停用**（直接发 `禁言`、`ping` 不再触发），所有命令都要加 `/` 前缀——避免普通聊天里的"在吗""状态"等词误触发机器人。

### 2. 禁言/踢出报错
| 群内提示 | 原因和解决 |
|---|---|
| `cannot ban admin` | 对方是管理员/群主，QQ 规则不允许，无法处理 |
| `not group admin` / 没有权限 | **机器人小号不是本群管理员**，请在群设置里把小号设为管理员 |
| `not member` | 对方不在本群 |

### 3. NapCat 掉线 / 要求扫码
- **现在基本不用管**：看门狗会自动拉起进程、自动重登（见第一节）
- 收到 `需要扫码登录.png` 时（说明 QQ 强制要求人工验证）：手机 QQ 扫这张图，选机器人小号授权，成功该文件自动消失
- 二维码约 2 分钟过期，文件会自动更新为最新码；也可打开 `http://127.0.0.1:6099/webui` 面板扫码
- 频繁掉线：检查网络；NapCat 版本过旧可按第六节升级；小号被风控时考虑换号

### 3.1 日志里看到「账号当前登录已失效」
这是 QQ 把你这个协议端踢下线了（常见于异地登录、风控、长时间运行）。看门狗的处理顺序：
1. 自动重启协议端尝试**免扫码**快速登录（每 3 分钟一次）
2. 若需要扫码：生成 `需要扫码登录.png` 等你扫（二维码约 2 分钟一换，等待期间不会重启进程打断扫码）
3. **连续自动重登 2 次仍失败**（`max_relogin_attempts` 可调）→ 放弃自动重试：停掉协议端与机器人服务、看门狗退出，`需要扫码登录.txt` 写明恢复步骤
4. 恢复方法：双击『启动机器人.bat』→ 出现二维码时用手机 QQ（机器人小号）扫码 → 登录成功后提醒文件自动消失、服务恢复正常

### 4. 广告拦截误伤正常发言
两种处理：
- 群里发 `广告列表` 找到命中的规则 → `删除广告 该规则`
- 临时方案：把相关成员设为管理员（管理员命中只提示不处理）

### 5. 广告没被拦截？
- 发消息的人是不是管理员/群主？→ 豁免机制，只提示不处理（设计行为）
- 关键词不在规则里？→ 群里发 `添加广告 关键词`
- 机器人是不是本群管理员？→ 不是的话撤回和禁言都会失败

### 6. 机器人发不出消息
- 被禁言了？（机器人自己被禁言时无法发言）
- 群是否开启了"全员禁言"？
- 检查机器人窗口是否有 `retcode` 报错，把完整报错发我排查

### 7. 入群审批没生效
- 检查门槛：群里发命令确认，如 `/设置等级 30`
- 等级查不到时按 fallback 策略处理（当前默认 ignore=转人工），改 `config.json` 的 `auto_approve.fallback` 为 `approve`/`reject` 可调整
- 注意 NapCat 的等级数据来自腾讯接口，偶尔有延迟

### 8. 启动时报端口被占 / 机器人连不上
- 最常见原因：**开了两个机器人实例**（比如工作区副本和桌面副本同时在跑，都抢 8080 端口）
- 解决：任务管理器结束所有 `python.exe`，只保留一个副本运行
- 只保留一份项目副本运行（把其他副本的 `python.exe` 全部结束后再启动）

### 9. 黑名单/设置的作用范围
- **黑名单是全局的**：在 A 群拉黑的人，进其他有机器人的群也会被自动踢出
- **设置命令改的是全局配置**：欢迎词、等级门槛、广告规则对所有群共用一份
- 如果以后需要按群独立配置，需要升级为按群存储（联系维护者改造）

---

## 三、配置与数据文件

| 文件 | 作用 | 改动方式 |
|---|---|---|
| `qq-group-bot/config.json` | 欢迎词、审批门槛、广告规则、违禁词 | 群命令（推荐）或直接编辑，即时生效 |
| `qq-group-bot/.env` | 端口、超级管理员QQ、命令前缀 `COMMAND_START` | 编辑后需重启机器人 |
| `qq-group-bot/data/blacklist.json` | 黑名单数据 | 用 `拉黑/解除拉黑` 命令管理，勿手改 |
| `qq-group-bot/data/reports/` | 未发言名单存档（每次查询自动生成 txt） | 只读，可随时删除 |

**重要数据只有两个**：`config.json`（含你调过的规则）和 `data/blacklist.json`（黑名单）。升级/重装前备份它们。

### 未发言名单命令说明

- 用法：群里发 `未发言`（默认30天）/ `未发言 60` / `未发言 90`；私聊发 `未发言 群号 30`
- 数据来源：协议端 `get_group_member_list` 的 `last_sent_time`（QQ 记录的最近发言时间），无需机器人长期在线积累
- 统计口径：超过 N 天没发言 + 从未发言；**入群不足 N 天的成员不计入**（单独提示数量）
- 人数 ≤15 直接发文本；>15 用合并转发，同时把完整名单写入 `data/reports/未发言_群号_N天_时间.txt`
- 名单里若含管理员/群主，会在末尾加提醒（避免误踢）

---

## 四、版本管理（git）

> v1.6.1 起，套件与机器人源码已合并为**单一 git 仓库**，仓库根目录就是 `QQ群管机器人/`（源码在 `qq-group-bot/` 子目录下），不再需要分两个仓库操作。

```bash
cd <项目目录>            # 例：cd /d/QQ群管机器人

git tag                     # 查看所有版本
git log --oneline           # 查看提交历史
git status                  # 看改了哪些文件

# 改完代码后的标准流程：
git add -A
git commit -m "说明这次改了什么"
git tag -a v1.x.x -m "版本说明"
```

**推荐：一键发布**（自动完成版本号更新、CHANGELOG 记录、提交打标，并推送到 GitHub 远程）：

```bash
cd <项目目录>            # 例：cd /d/QQ群管机器人
python deploy/release.py v1.6.2 "这次改动的说明"
```

- 只想本地提交、暂不推远程：加参数 `--no-push`
- 当前版本看 `VERSION` 文件；完整变更历史看 `CHANGELOG.md`
- **回滚到某个版本**：`git checkout v1.6.0 -- qq-group-bot/src/`（把源码恢复到当时状态）
- 版本历史：`v1.0.0-base` → `v1.1.0-deploykit` → `v1.2.0-maintenance` → … → `v1.6.1`

### GitHub 远程仓库（代码备份）

| 项 | 值 |
|---|---|
| 仓库地址 | https://github.com/GuiDI-666/qq-group-bot |
| 可见性 | 公开 |
| 默认分支 | `main` |

常用命令：

```bash
cd <项目目录>            # 例：cd /d/QQ群管机器人
git fetch origin            # 拉取远程分支信息（首次执行可消除分支状态提示）
git push origin main --follow-tags    # 推送提交与版本标签
git pull origin main        # 换机器后拉取最新代码
```

#### 隐私约定（重要，改仓库前先看）

仓库是**公开**的，所以个人配置一律不进 git，只提供模板：

| 本地文件（被 .gitignore 排除） | 仓库里的模板 |
|---|---|
| `部署配置.json` | `部署配置.example.json` |
| `qq-group-bot/.env` | `qq-group-bot/.env.example` |
| `qq-group-bot/config.json` | `qq-group-bot/config.example.json` |

- 也**不会**上传：`deps/`（离线依赖大包）、`logs/`（日志里有群号/QQ号）、`qq-group-bot/data/reports/`（名单存档）、`__pycache__`
- 改代码或写文档时不要写真实 QQ 号/群号，示例统一用 `1000123456`、`123456789` 这类假号
- 提交前自查：`git status` 里不应出现上述个人配置文件；`git ls-files | findstr /i "config.json .env"` 只应看到 `*.example`
- 仓库历史已在发布公开版时重建为单条初始提交（旧历史含日志，仅保留在本地备份），因此公开仓库没有历史包袱

---

## 五、升级

### 升级机器人代码
1. 备份 `config.json` 和 `data/blacklist.json`
2. 替换 `src/` 和 `bot.py`
3. `pip install -r requirements-full.txt`（如依赖有更新）
4. 重启，群里发 `ping` 验证

### 升级 NapCat
1. 去 `github.com/NapNeko/NapCatQQ/releases` 下载新版 `NapCat.Shell.Windows.Node.zip`
2. 备份 NapCat 目录下的 `NapCat\napcat\config\`（登录配置在里面，路径取决于 `部署配置.json` 的 `napcat_dir`）
3. 删除旧 `NapCat` 文件夹，解压新包，把备份的 config 放回去
4. 新版缺 `crypto.dll/ssl.dll` 的话，从 `deps/` 复制过去
5. 重启验证

> 不出问题不要主动升级；升级前务必看 release notes 是否有破坏性变更。

---

## 六、扩展开发（加新功能插件）

1. 在 `src/plugins/` 新建 `xxx.py`，最简模板：

```python
from nonebot import on_command
from nonebot.adapters.onebot.v11 import MessageEvent
from nonebot.params import CommandArg

hello = on_command("你好", priority=5, block=True)

@hello.handle()
async def _(event: MessageEvent, args = CommandArg()):
    await hello.finish(f"你好呀！")
```

2. 重启机器人，日志看到 `Succeeded to load plugin "xxx"` 即加载成功
3. 在 `src/plugins/help.py` 的菜单里补一行说明
4. git commit 留版本

---

## 七、应急处理

| 场景 | 操作 |
|---|---|
| 小号被风控/冻结 | 立即停止使用，换新小号：改 `部署配置.json` 的 `bot_qq` → 重跑 `一键部署.bat`（会生成新配置）→ 首次扫码登录 |
| 误拉黑某人 | 群里发 `解除拉黑 他的QQ号` |
| 配置改乱了 | `git checkout -- config.json` 恢复最近提交版本（黑名单数据不受影响） |
| 端口被占用 | 改 `部署配置.json` 的 `bot_port` 和 `ws_url`（两处端口要一致）→ 重跑部署 → 重启 |
| 电脑睡了一觉醒来机器人没反应 | 等 15 秒看门狗自愈；仍无反应就双击 `重新登录.bat`，或看根目录有没有 `需要扫码登录.png` |

---

## 八、部署到服务器

电脑睡眠/关机就等于机器人下线，长期稳定运行建议放到一直开机的机器（云服务器 / 小主机）。

### 8.1 Windows 服务器（推荐，与开发机同一套脚本）

把项目文件夹整个拷到服务器后，**先跑 `服务器部署.bat`，再跑 `一键部署.bat`**。

| 步骤 | 操作 | 说明 |
|---|---|---|
| 0 | 把源机器的 **`deploy` 整个文件夹** + **`服务器部署.bat`** 拷到服务器项目根目录（覆盖） | 少这两个文件，脚本会明确提示 |
| 1 | 双击 `服务器部署.bat` | 自动找 Python 3.9~3.13，没有就静默装 3.13；装依赖；把 `部署配置.json` 里属于旧机器的路径改成本机的；把 UTF-8 的 `.bat` 转成 GBK |
| 2 | 双击 `一键部署.bat` | 解压协议端 NapCat、写 `.env` 与 NapCat 网络配置、生成启动脚本、跑自检 |
| 3 | 双击 `启动机器人.bat` | 看门狗常驻；**首次登录要扫码**，二维码在项目根目录 `需要扫码登录.png` |
| 4 | 出问题 | 看 `server-deploy-log.txt`（部署过程）与 `deploy-report.txt`（环境体检） |

新增/变更的文件：

| 文件 | 作用 |
|---|---|
| `服务器部署.bat` | 服务器/新机器一键准备（纯 ASCII，无编码风险） |
| `deploy/server_prepare.py` | 环境体检 + 修正机器相关配置 + 处理失效 venv + 转 `.bat` 编码 |
| `deploy/deploy.py` | 新增按压缩包结构定位协议端、路径不合法时自动改默认值、依赖源自动换镜像 |

**8.1.1 为什么在服务器上双击会闪退**

`.bat` 是 UTF-8 编码、里面还写着 `chcp 65001`。cmd 执行时按字节偏移重读文件，
切换代码页后偏移错位，`echo` 的中文被拆成"命令"执行，脚本在报错前就退出了，
所以窗口一闪就没。**修法**：`.bat` 存成 GBK + `chcp 936`（`服务器部署.bat` 会自动做这件事）。

**8.1.2 其它两个坑（脚本已处理，知道原理便于排障）**

1. **协议端层级**：离线包 `deps/NapCat.Shell.Windows.Node.zip` 根目录直接就是 NapCat 本体，
   而程序约定协议端放在 `<napcat_dir>\NapCat\`。若不做区分，解压后 `node.exe` 位于错误层级，
   看门狗找不到它，机器人永远起不来。
2. **解释器探测器不能有重定向**：`call` 子程序里给候选解释器加 `>nul 2>nul`，
   当候选本身是 `.cmd/.bat` 时重定向不会被还原，会把父脚本后续所有输出吞掉
   （窗口看起来卡死）。现改为只认真正的 `.exe`，并靠退出码判断版本。

**8.1.3 机房 IP 提醒**

云服务器是机房 IP，非官方协议在机房 IP 上通常比家用宽带更容易被风控踢下线。
迁移后建议先观察一两天，别同时把大号也挂在同一台机器上。

### 8.2 Linux 服务器（规划中）

准备清单：

1. **Python 3.10+** + `pip install -r qq-group-bot/requirements.txt`
2. **NapCat Linux 版**：按官方文档安装（Docker 或 Linux 发布包均可），配置反向 WebSocket 指向 `ws://127.0.0.1:8080/onebot/v11/ws`，并开启 HTTP 服务（`部署配置.json` 里的 `napcat_http_port` / `napcat_http_token` 要与之对应）
3. **`部署配置.json`**：改 `napcat_dir` 指向 Linux 下的 NapCat 目录、`project_dir` 指向 `qq-group-bot`
4. **开机自启**：`python deploy/autostart.py install` 会生成 systemd 服务模板 `deploy/qq-group-bot.service`，按提示 `sudo cp` 到 `/etc/systemd/system/` 并 `systemctl enable --now qq-group-bot`
5. **已知待适配点**（当前代码在 Linux 上仍能跑守护逻辑，但这两处需按你的 NapCat 版本调整）：
   - `watchdog.py` 的 `start_napcat()`：Linux 分支默认执行 `napcat.sh`，如需免扫码快速登录要自行补 `-q 小号` 参数
   - 首次登录仍需扫码：把二维码从 NapCat 目录同步出来（看门狗会把码放到项目根目录，服务器上可用 `scp` 取回本地扫）
6. 服务器上注意：群白名单 `managed_groups`、私聊授权 `admin_users`、`config.json` 需一并复制过去

---

*最后更新：2026-09-18 · 配套版本 v1.7.2（含服务器部署套件）*
