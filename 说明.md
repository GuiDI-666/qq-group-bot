# QQ群管机器人 · 基础框架版 v1.0.0

独立于任何 AI 平台的 QQ 群管理机器人，NoneBot2 + OneBot V11（NapCat 协议端）。

## 快速开始（新机器部署）

> 首次部署或换新电脑，做这一步就够：

1. **准备配置文件**：把 `部署配置.example.json` 复制一份、改名为 `部署配置.json`，填入你的机器人QQ号、超级管理员QQ号、NapCat 安装目录
   （如果直接双击部署，脚本会自动从模板生成一份并提示你去填）
2. **双击 `一键部署.bat`**（自动完成：Python依赖安装 → VC++运行库 → NapCat解压 → DLL补齐 → 网络配置 → 生成启动脚本）
   - 需要电脑已安装 Python 3.9~3.13（安装时勾选 Add to PATH）
   - **如果这台机器上还没装 Python，或者是从别的电脑拷过来的项目**：先双击 **`服务器部署.bat`**，
     它会自动装好 Python 3.13、装依赖、把配置里指向旧机器的路径改成本机的、修正 `.bat` 编码，
     然后再双击 `一键部署.bat`。详见 `维护文档.md` 第八节。
3. 部署完成后**双击 `启动机器人.bat`**
4. 机器人小号首次登录需扫码，之后免扫码
5. 把小号拉进群并设为管理员，群里发 `菜单` 查看所有指令

> 隐私说明：仓库**不含任何真实账号信息**。`部署配置.json`、`qq-group-bot/.env`、`qq-group-bot/config.json` 三个个人配置文件已被 `.gitignore` 排除，仓库只提供对应的 `*.example` 模板。

> 日常启动只需双击 `启动机器人.bat`，不用再跑部署。
>
> 该脚本会启动**看门狗（watchdog）**：协议端或机器人进程掉线会自动拉起；账号登录失效会自动重新登录；电脑睡眠/休眠唤醒后自动体检自愈；只有“必须人工扫码”时才会在文件夹里生成 `需要扫码登录.png` 提醒你扫。
>
> 想让机器人开机就自动跑：双击 `开启开机自启.bat`（在「启动」文件夹放一个最小化快捷方式，登录 Windows 后自动运行）。

## 目录结构

```
QQ群管机器人/
├── 服务器部署.bat           服务器/新机器专用：自动装Python+装依赖+改本机路径+修bat编码
├── 一键部署.bat            新机器部署入口（幂等，可重复运行，结束自动自检）
├── 一键自检.bat            随时手动的9项部署自检
├── 部署配置.example.json     部署参数模板：机器人QQ/超级管理员/端口/NapCat路径（复制为 部署配置.json 后填写）
├── 启动机器人.bat           日常一键启动（看门狗模式：守护协议端+机器人，自动重登）
├── 重新登录.bat             协议端登录失效时手动触发重新登录
├── 开启开机自启.bat         登录系统后自动启动机器人（最小化窗口）
├── 关闭开机自启.bat         取消开机自启
├── 说明.md                  部署文档（本文件）
├── 维护文档.md              维护手册：启停/排障FAQ/版本管理/备份/升级/扩展开发
├── CHANGELOG.md             版本更新说明（每次发布记录，含完整版本历史）
├── VERSION                  当前版本号（如 1.5.0）
├── deploy/
│   ├── deploy.py            部署脚本（7步自动检查+安装+配置+自检）
│   ├── server_prepare.py    服务器/新机器准备（环境体检+改机器相关路径+处理失效venv+转bat编码）
│   ├── self_check.py        自检脚本（9项检查：环境/依赖/文件/配置/NapCat/运行状态）
│   ├── watchdog.py          看门狗：进程守护 + 登录失效自动重登 + 睡眠唤醒自愈 + 扫码提醒
│   ├── autostart.py         开机自启管理（install/uninstall/status；Linux 生成 systemd 模板）
│   └── release.py           版本发布工具（自动更新VERSION/CHANGELOG+提交打标+推送远程）
├── logs/                    运行日志（napcat.log / bot.log / watchdog.log）
├── 需要扫码登录.png/txt      仅在"必须人工扫码"时出现，扫码成功会自动消失
├── deps/                    离线依赖包（换机免重新下载）
│   ├── NapCat.Shell.Windows.Node.zip   NapCat v4.18.28 一体化包（111MB）
│   ├── vc_redist.x64.exe               VC++ 2015-2022 运行库
│   ├── crypto.dll / ssl.dll            QQ内核依赖的OpenSSL库
│   └── requirements-full.txt           Python依赖锁定版本清单
└── qq-group-bot/            机器人项目本体（NoneBot2 工程，随主仓库一同版本管理）
    ├── bot.py               入口
    ├── config.example.json  热配置模板（复制为 config.json；部署时会自动生成）
    ├── config.json          运行配置（欢迎词/等级门槛/广告规则/管理群，本地生成，不进仓库）
    ├── .env.example         环境变量模板（复制为 .env；部署时会自动生成）
    ├── .env                 服务配置（端口/超级管理员QQ，本地生成，不进仓库）
    ├── requirements.txt     核心依赖
    ├── data/
    │   └── blacklist.json   黑名单数据（自动生成）
    └── src/plugins/         10个功能插件
        ├── admin.py         禁言/解禁/踢出（支持@和裸QQ号）
        ├── blacklist.py     拉黑/解除拉黑/黑名单（自动踢+拒入群）
        ├── approval.py      入群自动审批（按QQ等级）
        ├── guard.py         广告/违禁词拦截（成员撤回+禁言；管理员仅提示）
        ├── welcome.py       入群欢迎/退群提示
        ├── settings.py      设置等级/广告规则/欢迎退群词（热生效）
        ├── inactive.py      未发言名单（30/60/90天，合并转发+存档）
        ├── status.py        存活检测（ping）
        ├── help.py          菜单
        └── common.py        公共工具
```

## 指令速查

| 类别 | 指令 | 说明 |
|---|---|---|
| 群管理 | `禁言 @/QQ号 [分钟]` | 默认10分钟，最长30天 |
| | `解禁 @/QQ号` | 解除禁言 |
| | `踢出 @/QQ号` | 仅群主可用 |
| 黑名单 | `拉黑 @/QQ号 [原因]` | 加黑名单并踢出、拒其再入群 |
| | `解除拉黑 @/QQ号` | 移出黑名单（别名：解黑） |
| | `黑名单` | 查看名单 |
| 设置 | `设置等级 数字` | 入群审批等级门槛（热生效） |
| | `添加广告 词/正则` | 追加广告规则（热生效） |
| | `删除广告 词/正则` | 删除规则 |
| | `广告列表` | 查看规则和违禁词 |
| | `设置欢迎 文本` | 欢迎语，`{at}`=@新人 |
| | `设置退群 文本` | 退群词，`{nickname}`=昵称 |
| 状态 | `存活/ping/状态` | 在线状态+运行时长 |
| 成员活跃 | `未发言 30/60/90` | 拉取超过N天未发言名单（默认30天；私聊：`未发言 群号 30`） |
| 其他 | `菜单/help` | 指令菜单 |

> 管理类指令需要 管理员/群主/超级管理员 身份。带 `/` 前缀或不带均可触发。

## 拦截规则说明

- **普通成员**命中广告/违禁词 → 消息撤回 + 禁言1小时
- **管理员/群主**命中 → 不撤回不禁言，仅群内提示"发言不规范"（豁免机制）
- 默认广告规则：微信号/QQ号/兼职刷单代购/扫码进群/裸链接，可随时用 `添加广告` 扩充

## 环境要求

- Windows 10/11 x64
- Python 3.9+（部署脚本自动装其余依赖）
- 网络能访问 pypi（依赖安装）；NapCat 与 DLL 均已离线打包在 deps/ 内

## 迁移到新电脑（完整清单）

1. 安装 Python 3.9+，勾选 Add to PATH
2. 把整个 `QQ群管机器人` 文件夹拷到新电脑
3. 修改 `部署配置.json`（bot_qq、superusers）
4. 双击 `一键部署.bat` → 完成后双击 `启动机器人.bat`
5. 机器人QQ首次登录需扫码（NapCat WebUI 可查看：`http://127.0.0.1:6099/webui`）

### 历史踩坑记录（部署脚本已自动处理）

- 缺 `msvcp140.dll` → 部署脚本检测并自动装 `deps/vc_redist.x64.exe`
- NapCat 报 `wrapper.node` 加载失败缺 `crypto.dll/ssl.dll` → 部署脚本自动从 deps/ 复制
- GitHub 下载慢 → NapCat 已离线打包，无需联网下载

## 注意事项

- 机器人小号走非官方协议（NapCat），存在风控风险，勿用于高频发言，建议专用小号
- 普通成员发广告会被撤回并禁言1小时；管理员/群主命中规则仅提示不处理
- NapCat WebUI：`http://127.0.0.1:6099/webui?token=8578ef2b2775`（本机）

---
版本历史：
完整变更记录见 **CHANGELOG.md**。概览：
- **v1.0.0-base**：基础框架定版（9插件）
- **v1.1.0-deploykit**：一键部署 + 离线依赖包
- **v1.2.0-maintenance**：部署自检 + 维护文档；修正NapCat反向WS配置键名
- **v1.2.1**：版本控制体系（CHANGELOG/VERSION/release.py 一键发布）
- 发布新版本：`python deploy/release.py v1.x.x "说明"`（自动更新版本号、变更记录、提交打标并推送 GitHub；加 `--no-push` 只本地提交）
- GitHub 仓库：https://github.com/GuiDI-666/qq-group-bot （公开；`deps/`、`logs/`、个人配置文件等都不进仓库，详见维护文档「四、版本管理」）
