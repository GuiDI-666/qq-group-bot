# 版本更新说明（CHANGELOG）

> 本项目的所有版本变更记录。新版本发布时在最上方追加。
> 格式参考：[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)
> 当前版本：**v1.8.0**（见 `VERSION` 文件）

## 发布流程（给维护者）

改完代码 → 更新本文件最上方（加新版本条目）→ 更新 `VERSION` → 提交打 tag（有远程则自动推送）：

```bash
python deploy/release.py v1.x.x "一句话说明"
```

（release.py 会自动完成：更新 VERSION、写入 CHANGELOG 模板条目、commit 并打同名 tag，配置了 origin 远程仓库时自动 push）

---

## [v1.8.0] - 2026-09-18

### 变更（⚠ 行为变更）
- **命令统一为 `/命令` 形式**：`command_start` 从 `{"", "/"}` 改为 `{"/"}`（`bot.py` 兜底 + `.env` + `.env.example` + 部署脚本同步修改）——直接发 `禁言`、`ping` 等裸命令**不再触发**，避免普通聊天里的"在吗""状态"等词误启动机器人
- 帮助菜单与所有命令用法提示同步改为 `/命令` 写法（help / admin / blacklist / settings / inactive / silence / bot.py 静默提示）
- 升级注意：`部署配置.json` 无需改动；只需更新 `qq-group-bot/bot.py`、`qq-group-bot/.env`（或其中的 `COMMAND_START` 行）与 `src/plugins/` 后重启机器人，之后所有命令都要加 `/`

## [v1.7.2] - 2026-09-18

### 变更
- **看门狗重登不再无限重试**：登录失效后自动重登最多尝试 2 次（`部署配置.json` 新增 `max_relogin_attempts` 可调），连续失败即放弃——停止协议端与机器人服务、看门狗退出，写入最终恢复提示，待管理员手动运行『启动机器人.bat』；避免反复自动重登加重风控、也让"需要人工"不被无限重试掩盖
- 移除旧的"3 次快速重试后每 15 分钟无限慢速重试"策略
- **服务器/新机部署套件**：新增纯 ASCII 的 `服务器部署.bat`（自动找/装 Python 3.13、装依赖多源回落）与 `deploy/server_prepare.py`（环境体检、修正机器相关配置、处理失效 venv、统一 bat 编码）
- 修复新机器部署三个坑：NapCat 离线包两种解压层级自动识别（`zip_top_layout`）、旧机器路径自动纠正、失效 venv 自动改名 `venv.machine-bound`
- 全部 `.bat` 统一为 GBK + `chcp 936` + CRLF（修复 UTF-8 中文 bat 在 cmd 下编码错位导致的双击闪退）

## [v1.7.0] - 2026-09-18

### 变更
- **开放源码（公开仓库）**：仓库重建为干净的单一初始提交，移除了历史提交中的运行日志（含群号/QQ号），并全面脱敏
- **个人配置模板化**：`部署配置.json`、`qq-group-bot/.env`、`qq-group-bot/config.json` 改为不入库（新增 `.gitignore` 规则），仓库只提供 `部署配置.example.json`、`.env.example`、`config.example.json` 三个模板
- 部署脚本支持从模板自动生成配置（首次运行 `一键部署.bat` 会提示填写；热配置自动从 example 复制）
- 文档与示例中的真实 QQ 号/群号/本机路径全部替换为占位值；`启动机器人.bat`、`重新登录.bat` 改为自动定位脚本目录（不再写死绝对路径）
- 看门狗/自检脚本的 NapCat 默认路径改为跟随当前用户目录

## [v1.6.1] - 2026-09-18

### 变更
- 项目整理为单一仓库（机器人源码并入 qq-group-bot/，保留完整开发历史备份）；新增 GitHub README；release.py 支持单仓发布并可自动推送远程

## [v1.6.0] - 2026-09-18

### 变更
- 看门狗增强：新增睡眠/休眠唤醒自愈（巡检间隔>60秒判定刚唤醒，立即体检——协议端在线则重启机器人刷新本地连接、离线或失效则立即重登），支持 --simulate-wake 测试；新增 deploy/autostart.py 开机自启管理（启动文件夹快捷方式，Linux 生成 systemd 模板）+ 开启/关闭开机自启.bat；外部实例占用端口/进程时改为只监控不重复启动（避免双开）；watchdog 增加 Linux 基础兼容；文档补充睡眠说明与服务器部署清单

## [v1.5.0] - 2026-09-18

### 变更
- 新增『未发言』潜水名单命令：支持 30/60/90 天（默认30天，私聊可指定群号），基于成员 last_sent_time 统计，入群不足周期的新人不计入；人数多时合并转发并落盘 data/reports/ 存档；帮助菜单与文档同步

## [v1.4.1] - 2026-09-18

### 变更
- 修复裸命令无反应的回归：deploy.py 写 .env 时补回 COMMAND_START=["","/"] 并保留自定义配置；bot.py 内置 command_start 兜底，避免部署覆盖后『ping/禁言/菜单』等无前缀命令失效

## [v1.4.0] - 2026-09-18

### 变更
- 新增看门狗(watchdog)：进程守护(掉线自动拉起) + 登录失效自动重登(免扫码优先) + 必须人工扫码时自动同步二维码到项目根目录；新增 重新登录.bat；启动机器人.bat 改为看门狗模式；NapCat 开启 HTTP 接口(3000)供状态检测；config/部署配置新增 napcat_http_port/token；同步运行时配置到桌面副本

## [v1.3.3] - 2026-09-18

### 变更
- 修复私聊管理命令无响应：新增 admin_users 私聊管理员名单（config.json，默认含大号和青松），管理群三命令授权范围=超级管理员+admin_users；未授权私聊保持静默

## [v1.3.2] - 2026-09-18

### 变更
- 两项逻辑收紧：1) 添加管理群/移除管理群/管理群列表 仅私聊可用（仅超级管理员），群聊内完全不响应；2) 新增机器人自身管理员身份检测（5分钟缓存）——机器人不是管理员的群对所有命令/自动功能完全静默

## [v1.3.1] - 2026-09-18

### 变更
- 管理群命令支持私聊+群号参数：群聊内只能操作本群（带其他群号会提示去私聊），私聊『添加管理群/移除管理群 群号』可管理任意群（仅超级管理员）；修复截图问题——原实现忽略参数导致误回复本群已在名单

## [v1.3.0] - 2026-09-18

### 变更
- 新增管理群白名单机制：仅在管理群内运行（命令/审批/拦截/欢迎/黑名单全自动功能全部受控），普通群完全不反应；新增 添加管理群/移除管理群/管理群列表 命令；菜单命令补上管理员权限限制

## [v1.2.2] - 2026-09-18

### 变更
- 修复guard异常处理吞掉finish信号、welcome黑名单用户欢迎语与踢人重复播报；维护文档新增双开端口冲突与黑名单/设置全局作用范围说明

## [v1.2.1] - 2026-09-18

### 变更
- 新增版本控制体系：CHANGELOG.md版本更新说明、VERSION版本号文件、release.py一键发布工具（双仓自动提交打标）

## [v1.2.0-maintenance] - 2026-09-18

### 新增
- **部署自检** `deploy/self_check.py`：9 项检查（Python版本/依赖/项目文件/配置格式/.env/插件加载/NapCat完整性/反向WS配置/运行状态），部署结束自动运行，也可通过 `一键自检.bat` 随时手动执行
- **维护文档.md**：日常启停、7类故障排查FAQ、配置说明、版本管理、升级流程、插件开发模板、应急预案

### 修复
- **重要**：`deploy.py` 写 NapCat 反向WS配置用错了键名 `reverseWebSocketClients`，NapCat 实际使用 `websocketClients` —— 原版脚本在新机器上部署会导致机器人连不上，已修正
- `welcome.py` 退群事件访问不存在的 `nickname` 属性导致崩溃，改用 `get_stranger_info` 查询昵称

## [v1.1.0-deploykit] - 2026-09-18

### 新增
- **一键部署**：`一键部署.bat` + `deploy/deploy.py`，7 步自动化（依赖安装→VC++运行库→NapCat解压→DLL补齐→网络配置→.env生成→启动脚本生成），幂等可重复运行
- **部署配置文件** `部署配置.json`：机器人QQ/超级管理员/端口/NapCat路径集中管理
- **离线依赖包** `deps/`：NapCat v4.18.28 一体化包（111MB）、vc_redist.x64.exe、crypto.dll/ssl.dll、requirements-full.txt（Python依赖锁定版本清单）——换机部署不再依赖 GitHub 下载

### 变更
- 项目整合到独立的 `QQ群管机器人/` 专用文件夹，整理目录结构

## [v1.0.0-base] - 2026-09-18

### 首个定版：基础框架（9 个插件）

| 插件 | 功能 |
|---|---|
| approval | 入群自动审批（QQ等级≥门槛自动通过，否则拒绝） |
| guard | 广告/违禁词拦截：普通成员撤回+禁言1小时；管理员仅提示不处理（豁免机制） |
| blacklist | 黑名单：拉黑即踢出、拒绝其再入群、进群自动踢 |
| admin | 禁言/解禁/踢出，支持 @某人 和裸QQ号两种格式 |
| welcome | 入群欢迎、退群提示 |
| settings | 设置等级/添加删除广告规则/设置欢迎退群词（热生效，无需重启） |
| status | 存活检测（ping/状态，显示运行时长） |
| help | 指令菜单（菜单/help/帮助） |
| common | 公共工具 |

### 运行环境
- NoneBot2 2.5.0 + OneBot V11 适配器 + NapCat v4.18.28 协议端
- 机器人小号与超级管理员QQ在 `部署配置.json` 中自行填写（仓库不含真实账号）

### 已知限制
- 小号走非官方协议，存在风控风险
- 管理员/群主发言不撤回不禁言（豁免为设计行为）
- 全局一份 config.json，多群共用设置（按群独立配置待后续版本）

---

*模板（发布新版本时复制）：*
```markdown
## [v1.x.x] - YYYY-MM-DD

### 新增
- 

### 变更
- 

### 修复
- 
```
