# 项目交接说明

> 写给在新项目/新会话中接手本项目的 AI 助理或新成员。读完这份即可接上进度。

## 当前状态（2026-09-18 10:40）

- **版本**：v1.7.0（见 `VERSION` / `CHANGELOG.md`）
- **运行状态**：在线正常。看门狗 10:04 启动，NapCat 协议端 + NoneBot 机器人都由它守护，群消息正常收发中
- **GitHub**：https://github.com/GuiDI-666/qq-group-bot （公开，已脱敏，单条干净历史 + v1.7.0 标签）
- **本地主仓库**：本项目根目录（即 `D:\Desktop\QQ群管机器人`），分支 `main`

## 项目是什么

基于 NoneBot2 + OneBot v11（NapCat 协议端）的独立 QQ 群管理机器人，不依赖任何智能体平台。功能：入群自动审批（按 QQ 等级）、广告拦截、禁言/踢出、黑名单、潜水名单（未发言 30/60/90 天）、欢迎语、帮助菜单、看门狗守护（进程守护 + 登录失效自动重登 + 睡眠唤醒自愈）。

## 关键文件

| 文件 | 说明 |
|---|---|
| `部署配置.json` | 部署参数（含真实 QQ 号，**不入库**，模板见 `部署配置.example.json`） |
| `qq-group-bot/config.json` | 运行配置：管理群白名单、管理员、审批门槛、广告规则（可用群命令热改） |
| `qq-group-bot/.env` | 端口、超级管理员（**不入库**，模板见 `.env.example`） |
| `qq-group-bot/data/blacklist.json` | 黑名单数据 |
| `deploy/watchdog.py` | 看门狗（常驻守护，`启动机器人.bat` 就是启动它） |
| `维护文档.md` | 维护手册：启停/排障 FAQ/版本管理/服务器部署 |
| `deps\_历史备份\` | 公开前的完整 git 历史 bundle（含真实信息，勿传仓库） |

## 常用操作

- 启动：双击 `启动机器人.bat`（看门狗模式）
- 状态：`python deploy\watchdog.py --status`，或群里发 `ping`
- 重新登录：双击 `重新登录.bat`；必须扫码时二维码会出现在根目录 `需要扫码登录.png`
- 发布新版本：`python deploy/release.py v1.x.x "说明"`（自动更新版本号/CHANGELOG/打标签并推 GitHub；加 `--no-push` 仅本地）

## 约定与注意

- **隐私约定**：真实 QQ 号、群号、令牌不进仓库和文档，示例统一用假号
- 机器人只在「管理群白名单 + 机器人是管理员」的群里响应；管理命令仅限私聊（管理员白名单见 config.json 的 `admin_users`）
- 非官方协议有封号风险，用小号跑
- 后续计划：迁移到 Linux 服务器部署（维护文档第八节有清单）
